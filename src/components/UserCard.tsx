import React from 'react';

const UserCard = ({type}:{type:string}) => {
    return (
        <div className='rounded-2xl odd:bg-[#d7e913]'>
            UserCard
        </div>
    );
};

export default UserCard;